<section style={{ background: "var(--bg)", position: "relative", overflow: "hidden" }}>
          <div className="tt-grid" style={{ position: "absolute", inset: 0, pointerEvents: "none" }} />
          <div style={{ position: "absolute", top: "-10%", right: "-8%", width: "clamp(300px,44vw,580px)", height: "clamp(300px,44vw,580px)", borderRadius: "50%", background: "radial-gradient(circle, var(--blob1) 0%, transparent 68%)", pointerEvents: "none" }} />
          <div style={{ position: "absolute", bottom: "-8%", left: "-6%", width: "clamp(180px,26vw,340px)", height: "clamp(180px,26vw,340px)", borderRadius: "50%", background: "radial-gradient(circle, var(--blob2) 0%, transparent 68%)", pointerEvents: "none" }} />

          <div className="tt-wrap" style={{ position: "relative", zIndex: 1 }}>
            <div className="tt-hero-grid">

              {/* Left */}
              <motion.div {...fadeUp(0)}>
                <div className="tt-pill tt-hero-pill" style={{ marginBottom: 22, width: "fit-content" }}>
                  <span className="tt-pls" style={{ width: 6, height: 6, borderRadius: "50%", background: "var(--ok)", display: "inline-block", flexShrink: 0 }} />
                  Now Accepting Bookings
                </div>
                <h1 className="tt-serif" style={{ fontSize: "clamp(2.5rem,5.2vw,3.9rem)", lineHeight: 1.08, color: "var(--tx1)", marginBottom: 18 }}>
                  Your Trusted<br />
                  <em style={{ color: "var(--accent)", fontStyle: "italic" }}>Electronics</em><br />
                  Service Partner
                </h1>
                <div className="tt-hero-rule" style={{ width: 52, height: 3, borderRadius: 2, background: "var(--accent)", marginBottom: 20, opacity: .7 }} />
                <p style={{ fontSize: ".97rem", lineHeight: 1.82, color: "var(--tx2)", marginBottom: 32, maxWidth: 460 }}>
                  Authorised multi-brand repair center serving{" "}
                  <strong style={{ color: "var(--tx1)", fontWeight: 600 }}>Peddapalli &amp; Mancherial</strong>{" "}
                  districts. TVs, ACs, washing machines &amp; refrigerators —
                  repaired right the first time, every time.
                </p>
                <div className="tt-hero-ctas" style={{ display: "flex", gap: 12, flexWrap: "wrap", marginBottom: 32 }}>
                  <a href="tel:+919876543210" className="tt-btn-p"><Phone size={15} /> +91 98765 43210</a>
                  <Link to="/services" className="tt-btn-g">Our Services <ArrowRight size={14} /></Link>
                </div>
                <div className="tt-hero-trust" style={{ display: "flex", gap: 18, flexWrap: "wrap" }}>
                  {["10+ Yrs Experience", "Genuine Parts", "Doorstep Service"].map((t, i) => (
                    <div key={i} style={{ display: "flex", alignItems: "center", gap: 6, fontSize: ".8rem", color: "var(--tx3)" }}>
                      <CheckCircle size={13} style={{ color: "var(--accent)", flexShrink: 0 }} /> {t}
                    </div>
                  ))}
                </div>
              </motion.div>

              {/* Right — stats */}
              <motion.div {...fadeUp(0.13)}>
                <div className="tt-card tt-dots" style={{ padding: 10 }}>
                  <div className="tt-g2" style={{ marginBottom: 10 }}>
                    {stats.map((s, i) => (
                      <motion.div key={i} {...fadeUp(0.1 + i * 0.07)} style={{
                        padding: "24px 16px", textAlign: "center", borderRadius: 10,
                        background: i % 2 === 0 ? "var(--surface)" : "var(--subtle)",
                        border: "1px solid var(--border)", position: "relative",
                      }}>
                        <div style={{ position: "absolute", top: 10, right: 10, width: 26, height: 26, borderRadius: 6, background: "var(--accent-lt)", border: "1px solid var(--accent-bd)", display: "flex", alignItems: "center", justifyContent: "center" }}>
                          <s.Icon size={12} style={{ color: "var(--accent)" }} />
                        </div>
                        <div className="tt-serif" style={{ fontSize: "clamp(1.7rem,3.2vw,2.3rem)", color: "var(--accent)", lineHeight: 1 }}>{s.value}</div>
                        <div className="tt-mono" style={{ fontSize: ".62rem", color: "var(--tx3)", marginTop: 7, letterSpacing: ".13em", textTransform: "uppercase" }}>{s.label}</div>
                      </motion.div>
                    ))}
                  </div>
                  <div className="tt-accent-surface" style={{ padding: "16px 18px", display: "flex", alignItems: "center", justifyContent: "space-between", flexWrap: "wrap", gap: 10 }}>
                    <div>
                      <div style={{ fontSize: ".85rem", fontWeight: 600, color: "var(--tx1)" }}>Same-Day Service</div>
                      <div style={{ fontSize: ".75rem", color: "var(--tx3)", marginTop: 2 }}>We come to you — call &amp; confirm</div>
                    </div>
                    <a href="tel:+919876543210" style={{ display: "inline-flex", alignItems: "center", gap: 6, background: "var(--accent)", color: "#fff", fontSize: ".78rem", fontWeight: 600, padding: "9px 15px", borderRadius: 8, textDecoration: "none", boxShadow: "0 3px 10px rgba(26,95,191,.26)", whiteSpace: "nowrap" }}>
                      <Phone size={12} /> Call Now
                    </a>
                  </div>
                </div>
              </motion.div>
            </div>
          </div>

          <div style={{ textAlign: "center", paddingBottom: 22, position: "relative", zIndex: 1 }}>
            <div className="tt-sc" style={{ display: "inline-flex", flexDirection: "column", alignItems: "center", gap: 5, color: "var(--tx4)" }}>
              <div style={{ width: 1, height: 28, background: "var(--border-str)", borderRadius: 2 }} />
              <span className="tt-mono" style={{ fontSize: ".58rem", letterSpacing: ".16em" }}>SCROLL</span>
            </div>
          </div>
        </section>